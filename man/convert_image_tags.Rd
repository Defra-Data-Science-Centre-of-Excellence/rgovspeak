% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/govspeak.R
\name{convert_image_tags}
\alias{convert_image_tags}
\title{Convert image tags in markdown to [Image: ...] format using renamed images}
\usage{
convert_image_tags(input_string, renamed_images)

convert_image_tags(input_string, renamed_images)
}
\arguments{
\item{input_string}{A character string containing markdown content with image tags to be converted.}

\item{renamed_images}{A data frame with columns \code{Original} and \code{New}, where \code{Original} contains
the original image filenames to search for, and \code{New} contains the new filenames to use in the replacement.}
}
\value{
The modified markdown string.

A character string with the image tags replaced by the new format.
}
\description{
This function searches for image tags in a markdown string that match filenames listed in a provided data frame,
and replaces them with a custom image reference using the new filenames.
}
\examples{
renamed_images <- data.frame(
  Original = c("old_image1.png", "old_image2.jpg"),
  New = c("new_image1.png", "new_image2.jpg"),
  stringsAsFactors = FALSE
)
input <- "Some text ![](path/old_image1.png)<!-- --> more text."
convert_image_tags(input, renamed_images)
}
